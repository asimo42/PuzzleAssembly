#pragma once
/*
#include <dshow.h>
#include <string>

class SoundPlayer
{
public:
	SoundPlayer(void);
	~SoundPlayer(void);
	int playSound(std::string filename);

private:
	IGraphBuilder *pGraph;
    IMediaControl *pControl;
    IMediaEvent   *pEvent;
	HRESULT hr;
};

*/